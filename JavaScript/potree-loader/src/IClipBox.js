import { Box3, Matrix4, Vector3 } from 'three-full'
const IClipBox = {
    box: new Box3(),
    inverse: new Matrix4(),
    matrix: new Matrix4(),
    position: new Vector3(),
}

export default IClipBox
